# pyIRCX runtime dependencies (server, API, and WebChat gateway).
# Ranges rather than exact pins so one file works on Python 3.9 through 3.14;
# upper bounds cap major versions that haven't been tested.
aiosqlite>=0.17,<1
bcrypt>=3.2,<6
pyotp>=2.6,<3
cryptography>=3.4
websockets>=10,<17
